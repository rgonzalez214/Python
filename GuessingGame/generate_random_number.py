import random


def return_random_number(random_number):

    # set the parameters for random.randint to be between -100 and 100 and save the value to random_number
    random_number = random.randint(-100, 100)
    # return the return_number when the function is called.
    return random_number
